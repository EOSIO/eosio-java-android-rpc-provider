# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
android.enableJetifier=true
android.useAndroidX=true
org.gradle.jvmargs=-Xmx1536m
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true

lib_group_name = one.block
lib_version = 0.1-alpha
lib_id = eosio-java-rpc-provider
lib_name = EOSIO SDK for Java: Android RPC Provider
lib_description = An Android RPC provider implementation for use within EOSIO SDK for Java as a plugin. RPC providers are responsible for all RPC calls to nodeos, as well as general network handling
lib_url = https://github.com/EOSIO/eosio-java-android-rpc-provider/
lib_license = MIT License
lib_license_url = https://github.com/EOSIO/eosio-java-android-rpc-provider/blob/master/LICENSE
lib_dev_id = block.one
lib_dev_name = Block One
lib_dev_email = dev@block.one

artifactory_url = http://localhost:8081/artifactory
artifactory_repo = test.maven
artifactory_username = anguyenqd
artifactory_password = 0ewT72wFbRvXaxw